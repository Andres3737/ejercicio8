apiVersion: v1 
kind: Service
metadata:
  name: password-ap
  labels:
    app: password-ap
spec:
  selector:
    app: password-ap
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 3000
  type: NodePort
